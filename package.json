{
  "name": "heinhtetaung-portfolio",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "dev:cf": "npm run build && wrangler pages dev dist",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=heinhtetaung-portfolio --branch=main"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "astro": "^7.0.7",
    "tailwindcss": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^4.110.0"
  }
}
